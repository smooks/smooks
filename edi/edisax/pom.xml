<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.milyn</groupId>
        <artifactId>milyn-edi</artifactId>
        <version>1.5-SNAPSHOT</version> <!-- base pom version - do not remove this comment -->
		<relativePath>../pom.xml</relativePath>
    </parent>
    <name>Milyn EdiSax</name>
    <groupId>org.milyn</groupId>
    <artifactId>milyn-edisax</artifactId>
    <version>1.5-SNAPSHOT</version> <!-- base pom version - do not remove this comment -->
    <packaging>pom</packaging>

	<properties>
		<milyn.edisax.model.version.base>${milyn.smooks.core.version.base}</milyn.edisax.model.version.base>
        <milyn.edisax.model.version>${milyn.edisax.model.version.base}-SNAPSHOT</milyn.edisax.model.version>
	</properties>

	<modules>
        <module>parser</module>
    </modules>

    <dependencies>
        <dependency>
            <groupId>org.milyn</groupId>
            <artifactId>testres</artifactId>
            <version>${milyn.smooks.core.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>codehaus.m2.snapshots</id>
            <url>http://snapshots.repository.codehaus.org</url>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
    </repositories>

</project>
