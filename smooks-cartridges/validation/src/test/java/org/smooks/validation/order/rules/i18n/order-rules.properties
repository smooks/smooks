# Order item rule messages...
valid_product_222_Qauntity=ftl:Order ${order.orderId} (Customer ${order.customerId}) contains an order item for product 222 which contains an invalid quantity of ${orderItem.quantity}. This quantity exceeds the maximum permited quantity for this product (5).