<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.milyn</groupId>
		<artifactId>smooks-examples-servlet-base</artifactId>
        <version>1.0</version>
	</parent>
	<name>Milyn Smooks Example - Servlet (Test)</name>
	<groupId>org.milyn</groupId>
	<artifactId>milyn-smooks-example-servlet-test</artifactId>
	<packaging>jar</packaging>
	<version>1.0</version>
	<dependencies>
		<dependency>
			<groupId>httpunit</groupId>
			<artifactId>httpunit</artifactId>
			<version>1.6.2</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.milyn</groupId>
			<artifactId>milyn-smooks-example-servlet</artifactId>
			<version>1.0</version>
			<type>war</type>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
				<executions>
					<execution>
						<id>surefire-it</id>
						<phase>integration-test</phase>
						<goals>
							<goal>test</goal>
						</goals>
						<configuration>
							<skip>false</skip>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<properties>
		<tomcat-home>E:/Development/java/app/tomcat/tomcat-5.5.26</tomcat-home>
	</properties>
	<profiles>
		<profile>
			<id>jetty6x</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.codehaus.cargo</groupId>
						<artifactId>cargo-maven2-plugin</artifactId>
						<version>1.0</version>
						<configuration>
							<wait>false</wait>
							<container>
								<containerId>jetty6x</containerId>
								<type>embedded</type>
							</container>
							<configuration>
								<home>${project.build.directory}/jetty6x/container</home>
								<deployables>
									<deployable>
										<groupId>org.milyn</groupId>
										<artifactId>milyn-smooks-example-servlet</artifactId>
										<type>war</type>
										<properties>
											<context>smooks-test</context>
										</properties>
									</deployable>
								</deployables>
                                <properties>
                                    <cargo.servlet.port>19191</cargo.servlet.port>
                                </properties>
							</configuration>
						</configuration>
						<executions>
							<execution>
								<id>start-container</id>
								<phase>pre-integration-test</phase>
								<goals>
									<goal>start</goal>
									<goal>deploy</goal>
								</goals>
							</execution>
							<execution>
								<id>stop-container</id>
								<phase>post-integration-test</phase>
								<goals>
									<goal>stop</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		
		<profile>
			<id>tomcat5x</id>
			<build>
				<plugins>
		            <plugin>
		                <artifactId>maven-antrun-plugin</artifactId>
		                <version>1.1</version>
		                <executions>
		                    <execution>
		                        <phase>pre-integration-test</phase>
		                        <configuration>
		                            <tasks>
		                                <ant dir="./" antfile="deploy.xml">
		                                	<property name="catalina.home" value="${catalina.home}"/>
		                                	<property name="catalina.deploy.url" value="${catalina.deploy.url}"/>
		                                	<property name="catalina.manager.uname" value="${catalina.manager.uname}"/>
		                                	<property name="catalina.manager.password" value="${catalina.manager.password}"/>
		                                </ant>
		                            </tasks>
		                        </configuration>
		                        <goals>
		                            <goal>run</goal>
		                        </goals>
		                    </execution>
		                </executions>
		            </plugin>
		        </plugins>			
			</build>
		</profile>
		
		<!-- Can't get it to work...
		<profile>
			<id>tomcat5x</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.codehaus.cargo</groupId>
						<artifactId>cargo-maven2-plugin</artifactId>
						<configuration>
							<wait>false</wait>
							<container>
								<containerId>tomcat5x</containerId>
							</container>
							<configuration>
								<type>existing</type>
								<home>E:/Development/java/app/tomcat/tomcat-5.5.26</home>							
							</configuration>
							
							<deployer>
								<deployables>
									<deployable>
										
										<groupId>org.milyn</groupId>
										<artifactId>milyn-smooks-example-servlet</artifactId>
										<type>war</type>
										<properties>
											<context>smooks-test</context>
										</properties>
										
										<pingURL>http://localhost:8080/smooks-test/index.html</pingURL>
									</deployable>
								</deployables>
							</deployer>
						</configuration>
						
						<executions>
							<execution>
								<id>start-container</id>
								<phase>pre-integration-test</phase>
								<goals>
									<goal>start</goal>
									<goal>deploy</goal>
								</goals>
							</execution>
							<execution>
								<id>stop-container</id>
								<phase>post-integration-test</phase>
								<goals>
									<goal>stop</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		 -->
	</profiles>
	<repositories>
		<repository>
			<id>milyn</id>
			<url>http://dist.codehaus.org</url>
		</repository>
	</repositories>
	<pluginRepositories>
		<pluginRepository>
			<id>codehaus snapshot repository</id>
			<url>http://snapshots.repository.codehaus.org/</url>
			<releases>
				<enabled>true</enabled>
			</releases>
		</pluginRepository>
	</pluginRepositories>
</project>
